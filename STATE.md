# STATE.md — single source of truth for build progress

## BUILD COMPLETE — all 19 phases (0-18) DONE ✅

### Phase checklist (§7)

- [x] **Phase 0 — Foundation.** Repo scaffold; framework core (http/html/db/auth/rbac/audit/events/jobs/settings/files); M1 items 1–5 (org onboarding, RBAC + matrix UI+doc, settings hierarchy, audit viewer, jobs dashboard); Simulator Console + Message Console; REST API v1 + keys + /developers + HMAC webhooks (M1.8); org-isolation tests. *Gate passed: all seeded personas log in; isolation suite green; 17 unit/integration + 4 e2e tests pass.*
- [x] **Phase 1 — Portfolio & units.** M2 complete: property/building/floorplan/unit CRUD with amenity premiums, rentable items, amenity spaces, unit status board+list with filters, property dashboard (occupancy/exposure/unit-mix donut/floorplan availability), portfolio roll-up comparison. Seed: Summit Ridge 220u/3 bldgs/6 plans, Foundry Lofts 150u, Cardinal Commons 24u student + property-scoped staff grants. *Gate passed: dashboards show live occupancy math; board filters verified in e2e; roll-up renders 3 properties. 20 tests + 5 e2e green.*
- [x] **Phase 2 — Ledger spine.** Standard multifamily chart of accounts + posting rules; balanced-JE core with closed-period blocking (dual-basis ready); charge engine with prorations (actual-days & 30-day), MTM premiums, idempotent monthly runs; resident/lease seed (362 leases, 650 residents, ~93% occupancy, statuses derived from leases); resident ledger UI with running balance; GL browser (trial balance/journal/entry) + live invariants page. *Gate passed in e2e: +7-day advance posted August rent org-wide; invariants green; ledger renders.*
- [x] **Phase 3 — Payments & receivables.** M8 complete: PaymentProcessor simulator (deterministic card auth/ACH returns from dials + test tokens), payment lifecycle pending→settled(T+3)→NSF/chargeback with reinstatement + fee + notification, settlement batches with processor fees, dual-basis GL postings incl. prepaid-credit tracking with auto-application, late fees (per-charge grace, flat/percent/daily w/ caps, preview screen, audited waivers), autopay job, payment plans with auto-charge + default handling, lockbox import, deposit accounting (held ledger, disposition apply→refund→collections, escrow cash split), delinquency workbench (buckets/notes/promises/dunning/plans/escalation/CSV), receivables analytics with 12-month trends. Seed: 14 months of history through the REAL pipelines — 5.6k charges, ~2.9k payments, 17k JEs, 637 settlement batches, 70 NSFs, 55%+ autopay, turnover leases with dispositions, Derrick Cole aged into 61–90. *Gate passed in e2e: pay→advance 3d→settled; late-fee preview→assess; workbench total = ledger truth; all 5 invariants green over full history.*
- [x] **Phase 4 — Resident portal core.** M7 items 1–3, 6, 9: mobile-first portal (bottom-tab shell), dashboard with balance hero/open requests/lease countdown/announcements/reservations, payments UX (one-time pay with saved methods + demo test instruments, convenience-fee messaging, roommate split suggestion, full ledger, monthly statement PDFs via pdf-lib), autopay enroll/cancel with confirmations, maintenance request intake (categories, photos, permission-to-enter, pet flag, preferred times, emergency keyword flagging) with resident-visible status timeline + completion ratings, lease self-service (docs, household info, policy-aware NTV that drives lease+unit status, household change requests with staff approval flow that adds pets/vehicles/occupants + pet rent). *Gate passed on 390px viewport in e2e: pay with saved method, autopay round-trip, photo request, statement PDF download.*
- [x] **Phase 5 — Facilities.** M10 complete: WO state machine with SLA clocks + emergency escalation to on-call, staff intake/triage/assign/schedule, materials (stock decrement + GL reclass 5910→5010/5020) & labor logging, tech My Day (mobile, signature-complete), drag-and-drop dispatch board, turn pipeline auto-created from notices (6-stage, vendor COI gating, cost roll-up to 5020, unit→vacant_ready on final QC), move-out processing job, PM schedules generating WOs on date advance + compliance stats, inspections (4 templates, move-in/out side-by-side compare, damages→resident ledger→deposit disposition), inventory with reorder flags, vendor registry with COI dispatch blocking, vendor portal, facilities analytics. Seed: 12 vendors (Pinnacle COI 12d out), ~600 historical + 36 open WOs, 28 turns in flight, PM schedules, stock. *All four gates green in e2e (30 e2e total).*
- [x] **Phase 6 — CRM & centralized leasing.** M3 complete: lead intake with email/phone dedupe into guest cards, full activity timelines, IlsFeed simulator delivering leads daily (dial-controlled, ~10% dedupe repeats), follow-up cadences (0/1/3/7/14) with auto-completion on outbound touches + overdue surfacing, tour scheduling with property-hours slots/conflicts/confirmations/day-before reminders/no-show handling, quote builder honoring term pricing + rentable items with emailed quotes, Leasing Center cross-property queue with round-robin + transfer-to-onsite, funnel analytics (conversion funnel, source ROI with spend inputs, agent leaderboard with first-response times), high-exposure floorplan urgency flags, call logging with fixture transcripts (ELI corpus). Seed: 401 leads/90d with realistic conversion, 180 tours, 116 quotes, 40 calls, Alicia Nguyen mid-funnel. *All gates green in e2e (37 e2e, 51 unit).*
- [x] **Phase 7 — Marketing websites & prospect portal.** M4 complete: per-property public sites (hero/gallery placeholders/amenities/neighborhood/floorplans) with LIVE availability + starting-at pricing, themed per property; prospect flows — request-info → M3 lead (deduped) and self-scheduled tours → M3 with confirmations, both mobile-perfect; Studio-style CMS (section toggles, text/image slots, theme color, meta description) with instant publish; SEO (meta/OG/JSON-LD ApartmentComplex, sitemap.xml, robots.txt); syndication manager (per-unit × channel toggles + publish-all, ListingPublication status); corporate brand site with city/beds/price search (stretch item done). *All gates green (43 e2e).*
- [x] **Phase 8 — Applications & screening.** M5 complete: tokenized multi-step application (save-and-resume, per-applicant invite links for co-applicants/guarantors/occupants, ID+income doc upload), fees at submission through the GL (per-adult app fee + refundable holding deposit), ScreeningBureau simulator (async turnaround via job/manual check, deterministic per-identity results incl. test identities, dial-shifted score mix, thin files), DocOcr income verification with anomaly review flags, duplicate-SSN fraud detection (flags never auto-decline), household scorecard vs versioned criteria (auto-snapshot on change; decisions stamped with version), approve/conditions/decline recommendations with guarantor rescue, audited overrides requiring screening:override + written reason, adverse-action letters (PDF + Message Console) on decline/conditions, unit holds with configurable window/expiry job + public availability exclusion + release/refund flows, quote→application one-click convert. Seed: 15 applications across every state incl. fraud + thin-file cases. *All gates green (49 e2e, 57 unit).*
- [x] **Phase 9 — Leases, e-signature & renewals.** M6 complete: versioned lease templates with merge fields + conditional addenda (pet/garage/student/MTM) and an admin designer; packet PDF assembly (template → merged PDF via pdf-lib, SHA-256 fingerprint); built-in e-sign — per-signer tokenized links, typed/drawn (canvas) signatures + initials, sequential countersignature enforcement, tamper-evident hash-chained event trail, executed packet = original + signature page (drawn PNGs embedded) + completion certificate, immutable + resident-visible; lease lifecycle state machine (draft→out_for_signature→partially_signed→fully_executed→active→renewed/ended) with activation job on move-in date — unit occupied, applicants→residents+portal accounts, deposit charge + holding-deposit credit through the GL, first-month prorations, move-in checklist (staff+portal views); renewals pipeline (30/60/90 buckets + MTM holdovers w/ premium billing, offer matrix honoring price-recommendation rows or capped % defaults, batch offers, portal accept/counter, accept→new lease at new rent→re-sign→rollover job); renewal activation carries the household's world forward: balance-forward AR↔AR transfer pair (GL-neutral), autopay enrollments, open work orders — and never re-charges the deposit or creates a move-in checklist. Quote→application→lease chain closed: **the golden path runs end-to-end in one e2e** (lead→tour→quote→application→screening→approve→generate lease→all parties sign→+30d advance→resident in portal with correct ledger→renewal offered→accepted→re-signed). *Gates green: 62 unit + 54 e2e (per-file DB isolation added to the harness). Screenshots in docs/screenshots/phase-9/.*
- [x] **Phase 10 — Accounting deep.** M9 complete: AP — vendor invoices with split GL coding (line-level property + capital project + cost code), amount-routed approvals (auto under threshold for approvers, else controller queue), payment runs (check/ACH) with org-wide check sequence, positive-pay register, void + reissue with full JE reversal; inter-property due-to/due-from automation (1300/2300) when one property pays another's bill, on both bases; BankFeed simulator — deterministic feed derived from the books (batch deposits net of escrow split, checks clearing on a 1-4 day lag, JE mirror for other cash events) plus bank-only reality (monthly processor fees in arrears, interest, noise) that requires real adjustment JEs; reconciliation workbench — auto-match by stable ref + amount/date tolerance, manual match/exclude, one-click adjustment JEs, complete-at-zero enforcement, book-vs-bank report (outstanding checks/deposits in transit), unreconciled-item aging; accounting periods with auto-evaluated close checklist (bank rec done, AP queue clear, JE approvals clear, recurring posted, subledgers tie, no stale settlements), closed-period posting blocked at postJE, audited reason-required reopen; manual JEs with amount-routed approval; recurring JEs (amortizations) posting on day-of-month with catch-up; budgets — per property/GL/year with monthly spread curves (even/seasonal/front/back), draft→approved versioning, budget-vs-actual variance flags, seed-from-actuals ±%; financial statements — balance sheet (balanced both bases, earnings roll-forward), income statement (period + T-12 matrix), direct-method cash flow, all per-property or consolidated, cash & accrual, CSV export; capital projects with cost-code budgets vs coded AP actuals. Seed: opening balances, 416 vendor invoices/13 payment runs through the real pipeline, insurance amortization RJEs, 42 account-months reconciled to zero (July intentionally open), 42 property-months closed, FY2026 budgets approved with variances, roof-replacement capital project, live AP approval queue. *Gates green: 73 unit + 60 e2e — accountant reconciles July to $0 live, closed June blocks a manual JE + audited reopen/re-close, BS balanced both bases, IS NOI ties to T-12 column, AR aging ties to control, budget variances flagged, AP approve→pay→void+reissue in UI. Screenshots in docs/screenshots/phase-10/.*
- [x] **Phase 11 — Utilities (M11) + Insurance & risk (M12).** M11: meter registry (593 meters incl. common) with SubmeterNetwork simulator reads (seasonal, sqft-scaled, dial-controlled spike/missed anomalies), anomaly review queue with accept/estimate (trailing-3-month rule); provider invoice management → invoices flow into AP (M9) with per-service GL coding, rate history, usage+cost trend dashboards with YoY and per-unit benchmarks; **RUBS engine** — per-property allocation formulas (submeter actuals / sqft / occupant-count / flat / hybrid), common-area deduction, per-bill billing fee, preview showing every unit's math with day-level occupancy proration, approve → converged charges post to resident ledgers (kind `utility`, idempotent month keys) and bill with rent; vacant-unit cost recovery — vacant shares stay property expense, tracked per unit/day with recovery-rate report; resident portal usage card (you vs community average + conservation nudges). M12: insurance compliance engine — third-party policy upload verified via InsuranceCarrier simulator (REJECT/SLOW test hooks, liability minimums), one-click master-policy enrollment (recurring program fee through the rent run), daily sweep expiring policies, 14/7/1-day lapse reminders (staged), lapse → **auto-enroll with notices** per org toggle; compliance dashboard (covered/lapsing/lapsed, master share) + risk page (coverage/lapse rates, incident log, claims); deposit alternative (monthly or one-time premium, coverage = deposit) — activation skips the traditional deposit, move-out disposition claims the surety up to coverage with remainder staying on the resident (GL-clean via 4110 funding); guaranty product — institutional guarantor attached to a conditions application rescues the scorecard (M5 hook), fee charges at move-in, guaranty + deposit-alternative addenda ride the lease packet (M6). Seed: utilities woven INTO the 14-month history via a month hook (reads → provider invoices → RUBS posted before payments sweep, so history stays convergent — 84 runs, ~5.2k utility charges, all invariants green), 259 third-party + 71 master policies, lapsing/lapsed demo buckets, 2 historical surety claims through real dispositions, guaranty on a conditions app, incident log. *Gates green: 79 unit + 67 e2e — RUBS preview→post live with per-unit math, converged Torres ledger, vacant recovery report, lapse auto-enroll clearing the bucket with notices in the console, July cycle staging on +8d advance, claims on /risk, guaranty-rescued scorecard, portal coverage+usage cards. Screenshots in docs/screenshots/phase-11/.*
- [x] **Phase 12 — Procure to pay.** M16 complete, wired to M10 inventory/vendors and M9 AP: purchase catalog (12 items with GL + inventory SKUs) + free-form lines with line-level GL/project/cost-code coding; POs with amount-routed approval chains (self-approve under threshold, pos:approve above), send → vendor portal acknowledgment, full/partial receiving that restocks M10 inventory and burns down job-cost commitments (open POs count against capital project budgets on /projects); DocOcr invoice prefill (deterministic extraction mirroring the PO/receipts, with a variance knob for demo exceptions); **2/3-way match** — invoice vs PO prices vs received value with a tolerance setting, no-receipt = automatic exception, exception queue with audited override (reason required, routes to AP) or reject; early-pay discount terms parsed from vendor terms ('2/10 net 30') stamping discount + deadline on invoices; Bill Pay riding Phase 10 payment runs, with remittance-advice PDFs and payment status in the vendor portal; **1099 workflow** — per-year eligible payments with the $600 floor, missing-W-9 exception list, downloadable summary PDF; spend analytics (by vendor/category/property, PO leakage, price-variance trend). Seed: 10-PO pipeline in every state — 5 received+matched+paid through the whole flow, one mis-priced invoice waiting in the exception queue, a pending-approval project PO committing the roof budget, one awaiting vendor ack, one partially received, one draft; SwiftTurn Painting missing a W-9. *Gates green: 86 unit + 75 e2e — PO→approve→vendor ack→receive→OCR invoice 3-way matches, the seeded exception overridden into AP, payment run pays it, vendor portal shows remittance PDF, 1099 summary + PDF generate. Screenshots in docs/screenshots/phase-12/.*
- [x] **Phase 13 — Communications complete.** M15: **unified per-person threads** — every outbound email/SMS auto-threads via a send hook on the messaging sim, inbound replies (simulated) land in the same conversation and flag needs-reply, calls (with transcripts) and internal notes merge into one timeline; shared inbox with needs-reply/assigned-to-me/snoozed views, assignment, snooze, close, click-to-log calls; **template library** — org overrides beat code defaults at render time (kept our-voice dunning override + a custom pool-closure template), custom templates with merge fields; **mass communications** — audience segments from live filters (property, balance > $X, lease expiring ≤ N days, has pet, autopay on/off) with exact per-recipient preview, scheduled sends via the day scheduler, per-recipient consent enforcement (skips recorded with reasons), unsubscribe links, and **quiet-hours SMS deferral** driven by a simulated clock-hour dial (defers at night, drains in the morning); announcements with optional email/SMS echo through the same consent-aware pipeline; **automation audit** — every lifecycle template with send counts, latest trigger link into the Message Console (new template/search filters), and per-org on/off toggles enforced inside notify(); portal communication-preferences card + public tokened unsubscribe. Seed: 500+ threads backfilled from 14 months of traffic, Maya's renewal counter-offer + Derrick's promise-to-pay waiting as inbound, opt-outs sprinkled, an autopay campaign scheduled for tomorrow, an announcement live. *Gates green: 90 unit + 81 e2e — the "balance > $0 at Summit Ridge" mass previews the exact live audience (UI count == service count), sends on the day advance into the console with opt-out skips recorded, night SMS defers then delivers at 10am, and a simulated inbound reply threads + flags needs-reply. Screenshots in docs/screenshots/phase-13/.*
- [x] **Phase 14 — Revenue intelligence.** M13 complete: **transparent pricing engine** — nightly job stages per-unit recommendations for vacant/on-notice units, every number explained by named factors with dollar deltas (floorplan exposure vs 7% target, days-vacant aging, seasonality, positioning vs the comp set, all clamped by a ±5% per-review guardrail; factors sum exactly to the recommendation — nothing is a black box, and no protected-class or occupant-derived inputs exist anywhere in the factor model); **comp market simulator** — 8 named comp communities across the 3 properties with deterministic monthly observations (stable per-comp bias ±8%, ~3.2%/yr drift, seasonal swings, occasional concessions), 14 months backfilled, us-vs-market positioning chart; **daily review queue** — accept / override-with-required-reason / reject / accept-all, audited; every decision writes `price_changes` (who/when/source/reason) and updates the unit's asking rent, so **M3 quotes, M4 public sites, and M6 renewal offers move the instant a decision lands** (quotes prefer accepted term rows; sites read live unit rents); **term-rate matrix with expiration smoothing** — terms 2-14 priced off a base curve (short-term premium → long-term discount) then steered by expiration-calendar load (p75-heavy months +2.5%, p25-light months −1.5%) with the calendar visualized; **renewal batch pricing** — one click prices every lease expiring in the window across 4 terms, capped at the org's `renewal_max_increase_pct` and never below current rent, rows land pre-accepted with the cap noted in their factor trail and M6 `renewalMatrix` consumes them directly; **revenue analytics** — loss-to-lease, new vs renewal trade-outs, effective-rent trend, trailing concessions. Seed: comp sets + 336 observations over 14 months, 8 weekly review cycles decided by the regional (318 accepted / 40 overridden / 32 rejected → a deep price_changes trail), 46 live pending recs, renewal batches staged for 2 properties (Foundry left unpriced so the demo can run it live). Also fixed a latent scoping bug this exposed: `agingRows` now property-scopes to the viewer, so the delinquency workbench never lists rows the viewer can't open. *Gates green: 96 unit + 87 e2e — queue factors sum to the shown number, an override drops the public site's "from" price and a fresh CRM quote in the same breath, accepting moves asking rent everywhere, the live Foundry batch keeps every term within the 8% cap (UI + DB checked), and the matrix visibly prices heavy expiration months above light ones. Screenshots in docs/screenshots/phase-14/.*
- [x] **Phase 15 — Reporting & BI complete.** M14 complete: **the entire §10 catalog — 50 reports** across Operations (Box Score, Rent Roll as-of-any-date, Availability & Exposure, Lease Expirations, Renewal Summary, Move Register, Notice & Turn Status, Occupancy Trend), Leasing & marketing (Funnel by Source, Source ROI, Agent Productivity, Application Pipeline & Screening Outcomes, Pricing Change History, Concession Usage), Receivables (Delinquency/Aged w/ notes, Collection Rate & On-Time %, NSF/Chargeback Register, Payment Plan Status, Prepaids & Credits, Deposit Accountability, Final Account Statement Register, Bad Debt/Write-Off), Accounting (Trial Balance, Balance Sheet, Income Statement period+T-12 both bases, GL Detail, Cash Flow, Budget vs Actual, AP Aging, Vendor Payment Register, 1099 Summary, Bank Rec Summary, Job Cost, Close Status), Facilities (WO Aging & SLA, Maintenance Cost/Unit, PM Compliance, Turn Time & Cost, Inspection Results, Inventory Valuation), Utilities/risk (Utility Recovery, Usage & Expense Trends, Vacant Recovery, Insurance Compliance, Deposit Alt & Guaranty), Portfolio/exec (Portfolio KPI Comparison, Economic vs Physical Occupancy, Revenue Analytics, Retention & Satisfaction, AI & Automation Activity) — **one engine** (`ReportDef`) gives every report the same parameter panel (property/date/range/basis), header-click sorting, group-by with subtotals, totals row, row drill-through to source records, CSV and PDF export; **as-of-date correctness** — effective-dated possession/balance/aging helpers (`asof.ts`) make operational reports reproducible for ANY historical date (payments that later NSF'd still count on the dates they were good; receivables outlive possession), plus **MetricSnapshot nightly rollups** (job + 15-month seed backfill) feeding trend reports; **custom report builder** — 9 datasets with code-defined column expressions (closed SQL surface), checkbox columns, typed filters (contains/=/≥/≤ with USD parsing), grouping with count+sum aggregates, fully linkable UI, save private/shared; **scheduled reports** — daily/weekly/monthly cadence on the day scheduler, CSV export via files, delivered to the owner's Message Console as an attachment link, "Run & deliver now" button; **role dashboards** — 12-widget library (KPI/trend/bar/table/funnel) with role-appropriate defaults (exec, PM, accountant, leasing, maintenance) and user-customizable layouts (add/remove/reorder/reset, persisted per user); **docs/metrics.md** defines every metric once. Carry-alongs the catalog exposed: real **bad-debt write-off flow** (negative AR charge → DR 5610/CR 1100, reason required, threshold-gated by gl:post, closes the collections case, workbench UI) and seed enrichment — move-in concessions (credit balances now exist), Derrick's promised payment plan + one more, 8 completed historical turn boards, 2 collection cases (1 written off). *Gates green: 105 unit + 93 e2e — all 50 reports render non-empty as admin and drill through; rent roll for 2026-01-26 (6 months back) reproduces the effective-dated truth exactly (row count + totals, since-departed residents included); a custom WO report built in the UI schedules daily and lands in the Message Console with a downloadable CSV attachment on the next day advance (alongside the seeded daily); trial balance sums to zero; snapshot cache == live computation; exec/maintenance role dashboards render and customize. Screenshots in docs/screenshots/phase-15/.*
- [x] **Phase 16 — AI layer.** M17 complete on **MockLlm** (deterministic LlmProvider; optional AnthropicLlm adapter reads ANTHROPIC_API_KEY (plus optional STAYLEASED_AI_MODEL), never required): **agent framework** — every action is an `ai_actions` row with full input/output/confidence/approval trail; per-agent per-property **autonomy dials** (draft-only / approve-to-send / autonomous-with-audit, layered code←org←property) with an AI Activity screen (approval queue with edit-before-send, audit history, dial matrix) — supervision is the product; low-confidence proposals hold even on autonomous. **Leasing AI** — grounded replies to prospect messages (live vacant-ready units, quotedRent 12-month pricing, pet policy from settings, real tomorrow tour slots), books tours into M3 on approval, hands off when a prospect asks for a human, after-hours coverage (clock-hour dial; Cardinal runs autonomous); wired to the live inbound-message hook AND a "✨ Draft AI reply" button on guest cards. **Maintenance AI** — intake triage on portal requests (keyword category/priority, unconditional emergency escalation for gas/CO/fire/flood, troubleshooting tips for trivial fixes like disposal resets, clarifying questions for vague reports), applies via triageWo on approval; Foundry triages autonomously. **Payments AI** — tone-graded outreach (friendly ≤15d, firm ≤45d, final beyond) with HARD-CODED compliance guardrails (banned-phrase filter, dispute path in every message, never threatens) + payment-plan proposals inside org bounds (≤4 installments, ≥$150 each) that create real M8 plans on approval. **Renewals AI** — outreach personalized from real history (on-time streak from ledger, WO satisfaction rating, matrix options); counter-offer evaluation against the approved band (2.5% below matrix): within → accept path that updates the offer + notifies, below floor → escalates to the PM with a thread note, never commits on its own. **ELI Call Analysis** — every transcript → summary/sentiment/intent tags on call_logs + action items as REAL followup_tasks + coaching notes + missed-opportunity flags; nightly job; rollup dashboard. **Essentials** — listing descriptions grounded in live pricing/amenities, template drafts, review responses, alt text; generate buttons in the CMS + template library + content studio. **Ask StayLeased** — topbar staff Q&A over org data through service APIs only (delinquency ≥ $X at property, expirations, occupancy, collections, pricing queue, WO backlog, vendor spend), property-scope enforced per asker, teach-the-syntax fallback, every ask audited. Seed: dials set (Cardinal leasing + Foundry maintenance autonomous), 40 calls analyzed → 9 AI follow-up tasks, 9 actions awaiting approval (Alicia's grounded reply+tour, Maya's $1,395 counter assessment, Derrick + Chen outreach/plans, gas + dishwasher triage, human-requested hold), 45 autonomous incl. an after-hours self-booked Cardinal tour. *Gates green: 112 unit + 99 e2e — at approve-to-send the queue card for Alicia quotes a verifiably real vacant unit + live price, one click sends the reply AND books the M3 tour; a live simulated inquiry flows through the hook into the queue; "I smell gas" triages emergency with the never-optional guardrail; 40/40 fixture transcripts carry summaries with real follow-up tasks; Ask StayLeased's three cross-module answers tie to DB truth exactly; AI Activity KPIs equal ai_actions counts. Screenshots in docs/screenshots/phase-16/.*
- [x] **Phase 17 — Verticals.** M18 complete as conditional behavior on Property.type — one codebase, no forks. **Student (FULL, live on Cardinal Commons):** by-the-bed **individual liability leases** (bed_label per bedspace, separate ledgers, double-booking blocked, non-student properties refused), academic-year terms from the org calendar (Aug 20 → Jul 31), **assignment board** (per-unit bed grid: current occupant vs fall assignee, inline assignment form creating the lease + resident + portal account), **parent/guarantor portal role** (guarantor user + banner'd read-and-pay portal view, NTV/household changes stay with the resident), **roommate matching** (5-question profiles, similarity scoring, greedy suggested groupings), **pre-lease pacing** (velocity curve vs a linear 95% target into fall — M13.7). Seed: 72 fall bed leases (75% pre-leased with a season-long creation curve), 36 parent guarantors, 8 questionnaires awaiting grouping. **Affordable (FULL workflow, Foundry LIHTC set-aside):** 20 program units across 50/60/80% AMI bands with utility allowances; deterministic **rent-limit schedule**; **income certification workflow** (5-item document checklist gating, income qualification against the band — over-income cannot certify) that **hard-blocks lease activation** until complete; over-limit rents blocked at activation, clamped in renewal offers/batches, and program units are never engine-priced; **annual recert job** (staggered due dates; 1 seeded overdue + 1 in progress); **audit-safe waitlist** (immutable positions, offers must follow order, skips demand written reasons, everything audited). **Military:** PCS-orders lease break on any lease detail — 30-day gate, orders document upload, notice set with **NO early-termination fee**, audited + resident confirmation; BAH reference schedule; allotments ride autopay. **Commercial:** CAM reconciliation worksheet (budgeted vs actual OpEx → per-suite true-up by sqft, preview mode without a commercial property). **Manufactured:** lot/home split + resident-owned + title/serial fields on units. Gaps logged in docs/parity.md (agency export formats, BAH-indexed rents, % rent, no commercial/manufactured demo property). *Gates green: 117 unit + 104 e2e — a bed is assigned in the UI, the individual lease activates on the day advance, the student sees their own ledger and the parent logs in as guarantor; an over-limit lease is refused at activation, the certification checklist completes in the UI (income-qualified) and activation unblocks; waitlist refuses out-of-order offers and demands documented skips; a PCS break posts zero fees. Screenshots in docs/screenshots/phase-17/.*
- [x] **Phase 18 — Hardening & handoff.** **Full regression from a fresh clone in one run:** `git clone` → `npm run seed` (63s deterministic world) → `npm run check` (strict tsc + **123 unit/integration tests**) → full Playwright suite (**104 e2e tests** covering every phase gate) — all green. **Performance pass:** hot-path indexes on insurance policies + charge due-dates cut the +30-day advance 51s → 38s; per-job timings profiled via job_runs (late-fee assessment is the steady-state leader at ~590ms/day — documented); every hot page renders < 500ms on seeded data (dashboards 461ms worst, most < 50ms). **Security review vs §9:** new sweep suite proves org isolation and permission guards across every M13–M18 surface (pricing decisions, AI approvals, custom report builder incl. SQL-injection-shaped filters, waitlist/certs/bed-assign/PCS, write-offs) — cross-org access reads as not-found, never forbidden-but-exists; plus a cross-org reference leak scan. **A11y sweep:** automated scan (lang, h1, img alt, empty buttons) across 18 key screens — error pages fixed (lang attr + h1), everything else clean; forms label via the shared field() helper throughout. **README demo tour:** a scripted 15-minute click-through of the entire platform (money → resident → ops → leasing → simulator day → pricing → reports-reproduce-history → verticals → Ask StayLeased), every step URL-verified per persona by script. **Docs:** parity.md fidelity map with per-vertical depth + logged gaps, screenshots gallery index (18 phase folders), metrics.md, permission matrix + ERD regenerated. *Gate passed: npm run check + full e2e green in one run from a fresh clone + seed; README tour verified step-by-step.*

## How to run right now

```bash
npm install         # no-op in the build sandbox (deps are symlinked); normal elsewhere
npm run seed        # deterministic demo world (npm run seed -- --reset to rebuild)
npm run dev         # http://localhost:3000
npm run check       # tsc --strict + unit/integration tests
npm run e2e         # Playwright smoke suite (fresh seeded db)
npm run shots -- 0  # capture phase screenshots
```

Demo logins (all password `demo1234`, see docs/demo-logins.md): `admin@summitridge.demo` (org admin), `regional@`, `manager@`, `agent@`, `maintsup@`, `tech@` (Sam Whitaker), `accountant@` (Priya Raman), `marketing@`, plus `platform@stayleased.demo`. Business date starts 2026-07-26; advance it in `/dev/sim`.

## Known gaps

- Message Console "simulate inbound reply" records messages but full threading arrives with M15 (Phase 13) per the §7 carry-along rule.
- Deposit escrow account (1020) is tracked at GL level; only operating accounts get a BankFeed/reconciliation in v1.
- Job costing commitments (PO-linked) wire up when M16 lands in Phase 12; 1099 summary report lands with M14 in Phase 15.
- Phase 11+ seed history must run BEFORE `seedAccounting` in parts.ts (it closes periods through last month).
- ESLint/Prettier unavailable in sandbox (tsc strict is the lint gate) — DECISIONS.md #10.

## Next tasks

None — the build is complete. The README's 15-minute tour is the handoff
walkthrough; docs/parity.md lists the intentional gaps if anyone extends
this. `npm run check && npm run e2e` from a fresh clone is the health bar.
